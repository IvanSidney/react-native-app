import React from "react";
import { Text, View } from "react-native";

export const BrowseListingScreen = ({}) => {
    return (
        <View
            style={{ flex: 1, justifyContent: "center", alignItems: "center" }}
        >
            <Text>Browse listing!</Text>
        </View>
    );
};
